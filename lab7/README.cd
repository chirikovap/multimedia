**Чирикова П.С. М8О-401Б-21**
**Лабоарторная 7**
  
Лабораторная работа решает задачу семантической сегментации с использованием модели Unet и предобученного энкодера ResNet34. Данные берутся из набора Pascal VOC, модель обучается с использованием функции потерь DiceLoss и оптимизатора Adam.

Зависимости

torch
segmentation_models_pytorch
albumentations
torchvision
numpy
matplotlib
sklearn
Установить через pip:

Обучение

Данные загружаются с использованием VOCSegmentation и аугментируются с помощью albumentations.
Модель Unet с энкодером ResNet34 обучается на данных с использованием DiceLoss и оптимизатора Adam.
Оценка

Для оценки используются метрики IoU и F1-Score. Результаты выводятся для каждого класса и в среднем по всем классам.

Результаты
После обучения и оценки модели, выводятся метрики для каждого класса:

Средний IoU по классам
Средний F1-Score по классам

Модель Unet с ResNet34 демонстрирует хорошие результаты для фона, но требует дальнейшей оптимизации для более точной сегментации других классов.
